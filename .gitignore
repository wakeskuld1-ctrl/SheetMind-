# 2026-03-23: 这里忽略 Rust 构建产物，原因是这些文件只属于本地编译缓存；目的是避免把体积大且可再生的二进制输出推到仓库。
target/

# 2026-03-23: 这里忽略本地运行时目录，原因是其中包含 Excel 临时副本、输出结果与运行态缓存；目的是让开源仓库只保留源码和可复用资产。
.excel_skill_runtime/
.worktrees/

# 2026-03-23: 这里忽略本地任务留痕目录，原因是其中包含本机开发轨迹与临时任务记录；目的是避免把个人过程性数据提交到公共仓库。
.trae/

# 2026-03-23: 这里忽略测试运行时产物，原因是这些文件由测试动态生成且可重复构建；目的是保持测试仓库内容稳定、可读。
tests/runtime_fixtures/
tests/runtime_outputs/
.playwright-cli/

# 2026-03-23: 这里忽略 Python 字节码缓存，原因是 desensitize 辅助脚本运行后会在本地产生缓存；目的是避免无意义缓存文件污染提交。
__pycache__/
*.pyc

# 2026-03-23: 这里忽略本地计划文件，原因是这些文件属于当前开发会话的过程文档；目的是把正式文档与临时过程材料分开管理。
findings.md
progress.md
task_plan.md

# 2026-03-23: 这里忽略 Windows 常见系统垃圾文件，原因是资源管理器可能自动生成；目的是减少跨机器协作时的无关差异。
Thumbs.db
